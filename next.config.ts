import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // experimental: {
  //   allowedDevOrigins: [
  //     "https://lazy-clouds-decide.loca.lt", // must match the origin in warning
  //   ],
  // },
};

export default nextConfig;